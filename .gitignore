# Runtime data — never commit recordings or transcripts
uploads/
outputs/
logs/

# Python virtualenv
.venv/
__pycache__/
*.pyc
*.pyo

# Server PID file
.server.pid

# Local config with API keys — never commit
config.json

# macOS metadata
.DS_Store
